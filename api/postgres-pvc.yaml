apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: analytics-postgres-pvc
  namespace: analytics-system
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: local-analytics
